

f64 main()
{
    f64 sum = 0.0;
    f64 flip = 1.0;
    i64 i = 1;
    while (i <= 1000000000) {
        flip = flip * -1.0;
        sum = sum + flip / (2*i - 1);
        i = i + 1;
    }
    return sum;
}