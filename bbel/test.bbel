
f64 main()
{
    f64 sum = 0.0;
    f64 flip = -1.0;
    i64 i = 1;
    while (i <= 1000000000) {
        flip = -flip;
        f64 f = ((2*i) - 1) as f64;
        sum = sum + (flip / f);
        i = i + 1;
    }
    return sum * 4.0;
}
